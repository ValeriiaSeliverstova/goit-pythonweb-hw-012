[pytest]
pythonpath = .
addopts = -q -ra
testpaths = tests
python_files = test_*.py *_test.py
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: tests that hit real DB or external systems
